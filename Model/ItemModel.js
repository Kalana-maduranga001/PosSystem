export class ItemModel {
    constructor(item, qty, price, description) {
         this.item = item;
         this.qty = qty;
         this.price = price;
         this.description = description;
    }
}